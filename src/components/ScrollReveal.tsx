import { useEffect } from "react";

// Minimal stub: in the original app this wires scroll/animation libs.
// For the UI-only workspace we keep layout identical and skip behavior.

export default function ScrollReveal() {
  useEffect(() => {
    // no-op on mount
  }, []);

  return null;
}
